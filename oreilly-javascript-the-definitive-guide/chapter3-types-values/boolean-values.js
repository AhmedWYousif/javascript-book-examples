// Boolean Values
a == 4;

if (a == 4) b = b + 1;
else a = a + 1;

// The following values convert to, and therefore work like, false:
undefined;
null;
0 - 0;
NaN;
(""); // the empty string

if ((x == 0 && y == 0) || !(z == 0)) {
	// x and y are both zero or z is non-zero
}

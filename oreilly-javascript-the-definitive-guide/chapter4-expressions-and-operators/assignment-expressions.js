
i = 0 // Set the variable i to 0.
o.x = 1 // Set the property x of object o to 1.

// assign and test a value in the same expression
(a = b) == 0

// Initialize 3 variables to 0
i = j = k = 0; 

total += sales_tax
// is equivalent to this one:
total = total + sales_tax


// Operator Equivalent
a += b      // a = a + b
a -= b      // a = a - b
a *= b      // a = a * b
a /= b      // a = a / b
a %= b      // a = a % b
a <<= b     // a = a << b
a >>= b     // a = a >> b
a >>>= b    // a = a >>> b
a &= b      // a = a & b
a |= b      // a = a | b
a ^= b      // a = a ^ b

data[i++] *= 2;
data[i++] = data[i++] * 2;
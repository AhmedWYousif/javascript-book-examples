var s = { x: 1, y: 1 }.toString();


var b = { x: 1, y: 1 }.toLocaleString();

new Date().toJSON();

// The valueOf() method is much like the toString() method, 
//but it is called when JS needs to convert an object to some primitive type other than a string
new Date().valueOf();

